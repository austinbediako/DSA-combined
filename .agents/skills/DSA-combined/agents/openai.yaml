interface:
  display_name: "DSA Combined"
  short_description: "Repo-specific patterns and workflows for DSA-combined"
  default_prompt: "Use the DSA-combined repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true